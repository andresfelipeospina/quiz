<form method='get' action='/quizes'>
	Búsqueda de quizes<p>
	<input type='text' name='search' value=''/>
	<input type='submit' value='Buscar'/>
	<table>
		<% for (var i = 0; i < quizes.length; ++i) {%>
			<tr>
				<td>
					<a href='/quizes/<%= quizes[i].id %>'><%= quizes[i].pregunta %></a>
				</td>
			</tr>
		<% } %>
	</table>
</form>